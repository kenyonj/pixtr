<h2><%= @group.name %></h2>
<% if signed_in? %>
  <div id="join-button">
    <%= render 'join_group_button', group: @group %>
  </div>
  <%= render 'like', subject: @group %>
<% end %>
<%= render @images %>
